name: zsh ドットファイルをリポジトリにdump
command: cp -f ~/.zshrc {{path}}.zshrc && cp -f ~/.zprofile {{path}}.zprofile
arguments:
  - name: path
    default_value: $(ghq list -p | grep Dot)/dotfiles/
    descroption: path to repositry

